apiVersion: v1
kind: Service
metadata:
  name: {{ include "health.fullname" . }}
  labels:
    {{- include "health.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "health.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.ports.http }}
      targetPort: {{ .Values.ports.http }}
