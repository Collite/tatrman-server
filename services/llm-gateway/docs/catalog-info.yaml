apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: llm-gateway
  description: LLM gateway — LLM gateway (Spring Boot); centralizes access to Azure OpenAI / Anthropic for the constellation
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: production
  owner: df-platform
  system: kantheon
  providesApis:
    - llm-gateway-api
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: llm-gateway-api
  description: OpenAPI definition for LLM gateway (LLM gateway)
spec:
  type: openapi
  lifecycle: production
  owner: user:guest
  definition:
    $text: openapi.json
