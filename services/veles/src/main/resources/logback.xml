<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
        <captureLoggerContext>true</captureLoggerContext>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="OTEL"/>
    </root>

    <logger name="org.tatrman.kantheon.ariadne" level="DEBUG"/>

    <!-- Transport/VCS libraries are noisy at DEBUG (gRPC's shaded Netty; JGit's clone/fetch on the
         GitHub model source). Pin them to INFO so they don't drown the app's own DEBUG output. -->
    <logger name="io.grpc.netty.shaded.io.netty" level="INFO"/>
    <logger name="io.netty" level="INFO"/>
    <logger name="org.eclipse.jgit" level="INFO"/>
</configuration>
