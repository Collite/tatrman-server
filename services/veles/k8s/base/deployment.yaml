apiVersion: apps/v1
kind: Deployment
metadata:
  name: veles
  namespace: kantheon
  labels:
    app: veles
spec:
  replicas: 1
  selector:
    matchLabels:
      app: veles
  template:
    metadata:
      labels:
        app: veles
    spec:
      containers:
        - name: veles
          image: veles:dev
          ports:
            - name: http
              containerPort: 7260
            - name: grpc
              containerPort: 7261
          env:
            - name: VELES_HTTP_PORT
              value: "7260"
            - name: VELES_GRPC_PORT
              value: "7261"
            - name: METADATA_GIT_REMOTE_URI
              value: "https://github.com/Tatrman/ai-models.git"
              # Override per cluster with the canonical fabric-infra secret.
            - name: METADATA_GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: veles-github-pat
                  key: token
                  optional: true
            - name: PROMPTS_GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: veles-github-pat
                  key: token
                  optional: true
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://alloy.observability.svc.cluster.local:4317
            - name: OTEL_SERVICE_NAME
              value: veles
            - name: OTEL_ENABLED_VELES
              value: "true"
          readinessProbe:
            httpGet:
              path: /ready
              port: 7260
            initialDelaySeconds: 2
            periodSeconds: 3
            failureThreshold: 20
          livenessProbe:
            httpGet:
              path: /health
              port: 7260
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1Gi
              cpu: 1000m
---
apiVersion: v1
kind: Service
metadata:
  name: veles
  namespace: kantheon
  labels:
    app: veles
spec:
  selector:
    app: veles
  ports:
    - name: http
      port: 7260
      targetPort: 7260
    - name: grpc
      port: 7261
      targetPort: 7261
