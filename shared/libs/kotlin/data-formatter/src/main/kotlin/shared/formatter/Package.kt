package shared.formatter

/**
 * data-formatter — converts Arrow IPC byte streams or JSON-rows arrays into JSON / CSV / TSV / Markdown.
 *
 * Public entry point: [DataFormatter].
 */
internal object Package
