# LM eval report

> GENERATED by `ttr-morph eval` — do not hand-edit.

Artifact `0.1.0`, content hash `sha256:bd73ed9feed72090ccb54c53c4f3902bcf6155dd235fc498f61b16acc70029fc`.

| | |
|---|--:|
| rows | 25097 |
| distinct forms | 24575 |
| fold collisions | 1400 |
| layers | core-hand (suite), core-cac (CC-BY-SA-4.0), core-kaikki (CC-BY-SA-4.0) |

## Named acceptance cases (S-7 → arc gate 7)

**3/3 pass.**

### ✓ `hero-compare` — the compare hero, with its diacritics

The sentence the demo is built on. Every content word resolves from the CORE artifact — no world layer, no statistical seam, no non-commercially-licensed model anywhere in the path.
`Kaufland` is the exception and the point: an entity name is OPEN vocabulary and belongs to a world layer (FI-1), so a core snapshot that answered here would be a core snapshot that had absorbed somebody's customer list. The miss is what feeds the enrichment loop (S-3); `03-hero-world.case.yaml` runs the same sentence with the world overlay and expects the answer.

| form | lemma | matched_via | provenance |
|---|---|---|---|
| Porovnej | porovnat | folded | lexicon |
| tržby | tržba | exact | lexicon |
| Kauflandu | — | — | — |
| za | za | exact | lexicon |
| loňský | loňský | exact | lexicon |
| rok | rok | exact | lexicon |
| s | s | exact | lexicon |
| letošním | letošní | exact | lexicon |

### ✓ `hero-folded` — the same hero, typed without diacritics

A Czech user on a foreign keyboard writes this, and it has to mean the same thing. Every accented word arrives through the fold index (contracts §5) and lands on the same lemma as case 1.
The law this case guards is the ORDERING: an exact hit is never reported as folded. *za*, *rok* and *s* exist verbatim in the artifact and must answer from themselves — the runner asserts that for every token of every case, so a future artifact that started resolving a real word through somebody else's fold key fails here rather than in a demo.

| form | lemma | matched_via | provenance |
|---|---|---|---|
| Porovnej | porovnat | folded | lexicon |
| trzby | tržba | folded | lexicon |
| Kauflandu | — | — | — |
| za | za | exact | lexicon |
| lonsky | loňský | folded | lexicon |
| rok | rok | exact | lexicon |
| s | s | exact | lexicon |
| letosnim | letošní | folded | lexicon |

### ✓ `nl-hero-lemma-path` — the NL hero's lemma path — NC-free

The primary NLS hero (`services/nlp/eval/corpus/rules.jsonl`, `hero-cs-invoices`). The rule pack that answers it matches on `lemma`, so the whole query turns on two lemmatizations: *faktury* -> `faktura` and *zákazníka* -> `zákazník`.
**This case is the NC-free proof.** Both come from the core lexicon, whose every layer is suite-licensed or CC BY-SA (contracts §10) — no MorfFlex, no PDT, no hunspell, nothing whose licence forbids commercial use (D-ε/Q-6). The option lane may reach the same lemmas through MorphoDiTa; this case asserts the DEFAULT lane can, with `provenance: lexicon` on every content word.
*Microsoft* is absent for the same reason *Kaufland* is: a customer name is world vocabulary.

| form | lemma | matched_via | provenance |
|---|---|---|---|
| Zobraz | zobrazit | folded | lexicon |
| všechny | všechno | exact | lexicon |
| faktury | faktura | exact | lexicon |
| od | od | exact | lexicon |
| zákazníka | zákazník | exact | lexicon |
| Microsoft | — | — | — |

## Target-vocabulary coverage

**147 of 149** (99%) target lemmas resolve to themselves.

Missing: `kvartál`, `největší`

## Corpus metrics

Not run — this invocation had no `--cac`. The committed numbers in `baseline.json` are the reference; see the module note in `harness.py` on why the release gate does not read the oracle.

