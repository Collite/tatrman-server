# Golden fixture: SPINE ONLY. The understanding layer (SV-P3·F1) disabled — proves
# the product installs as just the query path. Everything not listed keeps its
# values.yaml default (spine services stay enabled).
fuzzy: { enabled: false }
fuzzy-mcp: { enabled: false }
nlp: { enabled: false }
nlp-mcp: { enabled: false }
chrono: { enabled: false }
geo: { enabled: false }
money: { enabled: false }
grounding-mcp: { enabled: false }
resolver: { enabled: false }

# charon OFF (CH-P1, review-074·F9). Charon is spine muscle by the CH union ruling —
# but it is not on the QUERY PATH, which is the one claim this fixture makes. It moves
# bytes between engines; query → translate → validate → dispatch → worker never calls
# it. Same "not the query path" test that disables everything above. `full` and
# `dark-geo` carry charon's render coverage.
charon: { enabled: false }
