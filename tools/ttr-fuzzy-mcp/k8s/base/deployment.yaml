apiVersion: apps/v1
kind: Deployment
metadata:
  name: fuzzy-mcp
  namespace: kantheon
  labels:
    app: fuzzy-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fuzzy-mcp
  template:
    metadata:
      labels:
        app: fuzzy-mcp
    spec:
      containers:
        - name: fuzzy-mcp
          image: fuzzy-mcp:dev
          ports:
            - containerPort: 7267
          env:
            - name: MCP_FUZZY_SERVER_PORT
              value: "7267"
            # gRPC channel target → fuzzy service (contracts §7 ports 7265/7266).
            - name: FUZZY_GRPC_HOST
              value: fuzzy
            - name: FUZZY_GRPC_PORT
              value: "7266"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://alloy.observability.svc.cluster.local:4317
            - name: OTEL_SERVICE_NAME
              value: fuzzy-mcp
            - name: OTEL_ENABLED_FUZZY_MCP
              value: "true"
          readinessProbe:
            httpGet:
              path: /ready
              port: 7267
            initialDelaySeconds: 2
            periodSeconds: 3
            failureThreshold: 20
          livenessProbe:
            httpGet:
              path: /health
              port: 7267
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: fuzzy-mcp
  namespace: kantheon
  labels:
    app: fuzzy-mcp
spec:
  selector:
    app: fuzzy-mcp
  ports:
    - name: http
      port: 7267
      targetPort: 7267
