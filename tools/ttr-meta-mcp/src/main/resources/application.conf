server {
    port = "7262"
    port = ${?ARIADNE_MCP_SERVER_PORT}
    shutdownUrlPath = "/shutdown"
}

metadata {
    # Phase 2.1 (fork) — ariadne-mcp gRPC target.
    # `metadata.host` / `metadata.port` are the ariadne service's gRPC host/port;
    # defaults to the in-cluster service name. Override with `ARIADNE_GRPC_HOST`
    # and `ARIADNE_GRPC_PORT` to point at a dev cluster / port-forward.
    protocol = grpc
    protocol = ${?ARIADNE_PROTOCOL}
    host = "ariadne"
    host = ${?ARIADNE_GRPC_HOST}
    port = "7261"
    port = ${?ARIADNE_GRPC_PORT}
    path = ""
}

telemetry {
    enabled = false
    enabled = ${?OTEL_ENABLED_ARIADNE_MCP}
    otlp {
        endpoint = "http://localhost:4317"
        endpoint = ${?OTEL_EXPORTER_OTLP_ENDPOINT}
    }
}
