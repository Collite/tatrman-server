apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: postgres-worker
  description: Kotlin/JDBC worker that runs validated PlanNodes against PostgreSQL under per-tenant RLS; streams Arrow IPC results back via gRPC
  annotations:
    backstage.io/techdocs-ref: dir:.
  tags:
    - kotlin
    - worker
    - postgres
    - jdbc
    - rls
    - v1
spec:
  type: service
  lifecycle: production
  owner: df-platform
  system: kantheon
