# Gradle
.gradle/
build/
**/build/
**/bin/

# IntelliJ / IDEA
.idea/
*.iml
out/
.run/

# Kotlin / JVM
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
# Hebe plugins-module test fixture: a built `hello-plugin` PF4J jar the plugin
# load/isolation specs read from the classpath (P1 Stage 1.1). Regenerated from
# `agents/hebe/plugin-template` (and at Stage 1.2 after the package rename).
!agents/hebe/modules/plugins/src/test/resources/hello-plugin.jar
*.war
*.ear
.kotlin/

# JavaScript / Vue (frontends/iris)
**/dist/
**/node_modules/
.eslintcache
/.npm-cache/

# Python
**/__pycache__/

# VS Code
.vscode/

# macOS
.DS_Store

# Secrets — never commit
**/*.secrets.env
**/*.private.env.json
*.kv.env

# Generated proto outputs are under build/; covered above.

# Local working files
/.output.txt
/graphify-out/
/debugging/

# Generated eval reports (routing + resolution harnesses)
agents/themis/eval/results/

# Backup files
*.bak
*.orig

# Helm: vendored library/subchart deps are rebuilt by scripts/helm-deps.sh
# (per-service charts + the SV-P4 umbrella). The golden/ output IS committed.
**/k8s/charts/
**/k8s/Chart.lock
helm/tatrman-server/charts/
helm/tatrman-server/Chart.lock

# RG-P1.S1 — ttr-nlp Python proto stubs (regenerated from shared/proto via
# services/ttr-nlp/scripts/gen_proto.py; canonical source is the .proto file).
services/ttr-nlp/generated/
